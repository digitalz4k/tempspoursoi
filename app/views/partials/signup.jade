.auth-container
    .row
        section
            h2.text-center Créer un compte
            form.form(class="large-4 large-centered columns", name='form', ng-submit='register(form)', novalidate='')
                .form-group(ng-class='{ \'has-success\': form.name.$valid && submitted, \'has-error\': form.name.$invalid && submitted }')
                    input(type="text", name='name', ng-model='user.name', placeholder="nom", required='')
                    p.help-block(ng-show='form.name.$error.required && submitted')
                        | Un nom est requis
                .form-group(ng-class='{ \'has-success\': form.email.$valid && submitted, \'has-error\': form.email.$invalid && submitted }')
                    input(type='email', name='email', ng-model='user.email', placeholder="email", required='', mongoose-error='')
                    p.help-block(ng-show='form.email.$error.email && submitted')
                        | L'adresse ne semble pas valide.
                    p.help-block(ng-show='form.email.$error.required && submitted')
                        | Quel est votre email ?
                    p.help-block(ng-show='form.email.$error.mongoose')
                        | {{ errors.email }}

                .form-group(ng-class='{ \'has-success\': form.password.$valid && submitted, \'has-error\': form.password.$invalid && submitted }')
                    input(type='password', name='password', ng-model='user.password', ng-minlength='3', placeholder="mot de passe", required='', mongoose-error='')
                    p.help-block(ng-show='(form.password.$error.minlength || form.password.$error.required) && submitted')
                        | 3 charactères minimum
                    p.help-block(ng-show='form.password.$error.mongoose')
                        | {{ errors.password }}
                .form-group.text-center
                    button.secondary(type='submit') S'inscrire
            p.text-center.small Déjà un compte ? 
                a.text-center.new-account(href='/login') Se connecter
div(ng-include='\'partials/footer\'')